import SwiftUI

/// The iOS application entry point.
@main
struct iOSApp: App {
    /// Declares the app scene graph and installs the root content view.
    var body: some Scene {
        WindowGroup {
            ContentView()
        }
    }
}